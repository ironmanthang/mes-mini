generator client {
  provider        = "prisma-client-js"
  output          = "../src/generated/prisma"
  previewFeatures = ["driverAdapters"]
}

datasource db {
  provider = "postgresql"
}

model Employee {
  employeeId             Int                     @id @default(autoincrement()) @map("employee_id")
  fullName               String                  @map("full_name")
  username               String                  @unique
  password               String
  createdAt              DateTime                @default(now()) @map("created_at")
  updatedAt              DateTime                @updatedAt @map("updated_at")
  email                  String                  @unique
  hireDate               DateTime                @map("hire_date")
  phoneNumber            String                  @unique @map("phone_number")
  status                 EmployeeStatus          @default(ACTIVE)
  terminationDate        DateTime?               @map("termination_date")
  address                String?
  dateOfBirth            DateTime?               @map("date_of_birth")
  roles                  EmployeeRole[]
  inventoryTransactions  InventoryTransaction[]
  approvedMaterialReqs   MaterialExportRequest[] @relation("Approver")
  createdMaterialReqs    MaterialExportRequest[] @relation("Requester")
  notifications          Notification[]
  createdProductionReqs  ProductionRequest[]
  approvedPurchaseOrders PurchaseOrder[]         @relation("POApprover")
  createdPurchaseOrders  PurchaseOrder[]         @relation("POCreator")
  attachments            Attachment[]
  performedQualityChecks QualityCheck[]
  approvedSalesOrders    SalesOrder[]            @relation("SOApprover")
  managedSalesOrders     SalesOrder[]
  createdStocktakes      Stocktake[]             @relation("StocktakeCreator")
  createdWorkOrders      WorkOrder[]

  @@index([status])
  @@map("employees")
}

model Role {
  roleId    Int            @id @default(autoincrement()) @map("role_id")
  roleName  String         @unique @map("role_name")
  createdAt DateTime       @default(now()) @map("created_at")
  updatedAt DateTime       @updatedAt @map("updated_at")
  employees EmployeeRole[]

  @@map("roles")
}

model EmployeeRole {
  employeeId Int      @map("employee_id")
  roleId     Int      @map("role_id")
  employee   Employee @relation(fields: [employeeId], references: [employeeId], onDelete: Cascade)
  role       Role     @relation(fields: [roleId], references: [roleId], onDelete: Cascade)

  @@id([employeeId, roleId])
  @@map("employee_roles")
}

model Agent {
  agentId     Int          @id @default(autoincrement()) @map("agent_id")
  agentName   String       @map("agent_name")
  phoneNumber String?      @unique @map("phone_number")
  email       String?      @unique
  address     String
  createdAt   DateTime     @default(now()) @map("created_at")
  updatedAt   DateTime     @updatedAt @map("updated_at")
  code        String       @unique
  salesOrders SalesOrder[]

  @@index([agentName])
  @@map("agents")
}

model Supplier {
  supplierId         Int                 @id @default(autoincrement()) @map("supplier_id")
  supplierName       String              @map("supplier_name")
  phoneNumber        String?             @unique @map("phone_number")
  email              String?             @unique
  address            String
  createdAt          DateTime            @default(now()) @map("created_at")
  updatedAt          DateTime            @updatedAt @map("updated_at")
  code               String              @unique
  purchaseOrders     PurchaseOrder[]
  suppliedComponents SupplierComponent[]

  @@index([supplierName])
  @@map("suppliers")
}

model Customer {
  customerId   Int        @id @default(autoincrement()) @map("customer_id")
  customerName String     @map("customer_name")
  phoneNumber  String?    @unique @map("phone_number")
  email        String?    @unique
  createdAt    DateTime   @default(now()) @map("created_at")
  updatedAt    DateTime   @updatedAt @map("updated_at")
  address      String?
  warranties   Warranty[]

  @@index([customerName])
  @@map("customers")
}

model ProductCategory {
  categoryId   Int       @id @default(autoincrement()) @map("category_id")
  categoryName String    @map("category_name")
  description  String?
  createdAt    DateTime  @default(now()) @map("created_at")
  updatedAt    DateTime  @updatedAt @map("updated_at")
  products     Product[]

  @@map("product_categories")
}

model Product {
  productId          Int                 @id @default(autoincrement()) @map("product_id")
  productName        String              @map("product_name")
  categoryId         Int?                @map("category_id")
  minStockLevel      Int                 @default(0) @map("min_stock_level")
  createdAt          DateTime            @default(now()) @map("created_at")
  updatedAt          DateTime            @updatedAt @map("updated_at")
  code               String              @unique
  unit               String
  bom                BillOfMaterial[]
  productInstances   ProductInstance[]
  productionRequests ProductionRequest[]
  category           ProductCategory?    @relation(fields: [categoryId], references: [categoryId])
  salesOrderDetails  SalesOrderDetail[]
  workOrders         WorkOrder[]

  @@index([productName])
  @@map("products")
}

model Component {
  componentId           Int                     @id @default(autoincrement()) @map("component_id")
  componentName         String                  @map("component_name")
  description           String?
  createdAt             DateTime                @default(now()) @map("created_at")
  updatedAt             DateTime                @updatedAt @map("updated_at")
  code                  String                  @unique
  minStockLevel         Int                     @default(0) @map("min_stock_level")
  standardCost          Decimal                 @default(0) @map("standard_cost") @db.Decimal(10, 2)
  unit                  String
  bom                      BillOfMaterial[]
  componentStocks          ComponentStock[]
  inventoryTransactions    InventoryTransaction[]
  materialReqDetails       MaterialRequestDetail[]
  purchaseOrderDetails     PurchaseOrderDetail[]
  productionRequestDetails ProductionRequestDetail[]
  stocktakeItems           StocktakeItem[]
  suppliedBy               SupplierComponent[]
  componentLots            ComponentLot[]

  @@index([componentName])
  @@map("components")
}

model BillOfMaterial {
  productId      Int       @map("product_id")
  componentId    Int       @map("component_id")
  quantityNeeded Int       @map("quantity_needed")
  component      Component @relation(fields: [componentId], references: [componentId], onDelete: Cascade)
  product        Product   @relation(fields: [productId], references: [productId], onDelete: Cascade)

  @@id([productId, componentId])
  @@map("bill_of_materials")
}

model PurchaseOrder {
  purchaseOrderId       Int                    @id @default(autoincrement()) @map("purchase_order_id")
  orderDate             DateTime               @default(now()) @map("order_date")
  employeeId            Int                    @map("employee_id")
  supplierId            Int                    @map("supplier_id")
  createdAt             DateTime               @default(now()) @map("created_at")
  updatedAt             DateTime               @updatedAt @map("updated_at")
  deliveryTerms         String?                @map("delivery_terms")
  expectedDeliveryDate  DateTime?              @map("expected_delivery_date")
  paymentTerms          String?                @map("payment_terms")
  priority              Priority               @default(MEDIUM)
  shippingCost          Decimal                @default(0) @map("shipping_cost") @db.Decimal(15, 2)
  taxRate               Decimal                @default(0) @map("tax_rate") @db.Decimal(5, 2)
  warehouseId           Int                    @map("warehouse_id")
  code                  String                 @unique
  totalAmount           Decimal                @default(0) @map("total_amount") @db.Decimal(15, 2)
  note                  String?
  approvedAt            DateTime?              @map("approved_at")
  approverId            Int?                   @map("approver_id")
  status                PurchaseOrderStatus    @default(DRAFT)
  inventoryTransactions InventoryTransaction[]
  details               PurchaseOrderDetail[]
  approver              Employee?              @relation("POApprover", fields: [approverId], references: [employeeId])
  employee              Employee               @relation("POCreator", fields: [employeeId], references: [employeeId])
  supplier              Supplier               @relation(fields: [supplierId], references: [supplierId])
  warehouse             Warehouse              @relation(fields: [warehouseId], references: [warehouseId])

  @@map("purchase_orders")
}

model PurchaseOrderDetail {
  purchaseOrderId     Int                @map("purchase_order_id")
  componentId         Int                @map("component_id")
  unitPrice           Decimal            @map("unit_price") @db.Decimal(10, 2)
  poDetailId          Int                @id @default(autoincrement()) @map("po_detail_id")
  quantityOrdered     Int                @map("quantity_ordered")
  quantityReceived    Int                @default(0) @map("quantity_received")
  productionRequestId Int?               @map("production_request_id")
  component           Component          @relation(fields: [componentId], references: [componentId])
  purchaseOrder       PurchaseOrder      @relation(fields: [purchaseOrderId], references: [purchaseOrderId], onDelete: Cascade)
  productionRequest   ProductionRequest? @relation(fields: [productionRequestId], references: [productionRequestId])
  componentLots       ComponentLot[]

  @@unique([purchaseOrderId, componentId])
  @@map("purchase_order_details")
}

model ComponentLot {
  componentLotId      Int                 @id @default(autoincrement()) @map("component_lot_id")
  lotCode             String              @unique @map("lot_code")
  componentId         Int                 @map("component_id")
  poDetailId          Int                 @map("po_detail_id")
  warehouseId         Int                 @map("warehouse_id")
  quantity            Int
  receivedAt          DateTime            @default(now()) @map("received_at")
  createdAt           DateTime            @default(now()) @map("created_at")

  component           Component           @relation(fields: [componentId], references: [componentId])
  poDetail            PurchaseOrderDetail @relation(fields: [poDetailId], references: [poDetailId])
  warehouse           Warehouse           @relation(fields: [warehouseId], references: [warehouseId])

  @@map("component_lots")
}

model SalesOrder {
  salesOrderId        Int                 @id @default(autoincrement()) @map("sales_order_id")
  orderDate           DateTime            @default(now()) @map("order_date")
  employeeId          Int                 @map("employee_id")
  agentId             Int                 @map("agent_id")
  createdAt           DateTime            @default(now()) @map("created_at")
  updatedAt           DateTime            @updatedAt @map("updated_at")
  approvedAt          DateTime?           @map("approved_at")
  approverId          Int?                @map("approver_id")
  code                String              @unique
  deliveryTerms       String?             @map("delivery_terms")
  discount            Decimal             @default(0) @db.Decimal(15, 2)
  expectedShipDate    DateTime?           @map("expected_ship_date")
  note                String?
  paymentTerms        String?             @map("payment_terms")
  priority            Priority            @default(MEDIUM)
  agentShippingPrice  Decimal             @default(0) @map("shipping_cost") @db.Decimal(15, 2)
  tax                 Decimal             @default(0) @db.Decimal(15, 2)
  totalAmount         Decimal             @default(0) @map("total_amount") @db.Decimal(15, 2)
  courierShippingCost Decimal             @default(0) @map("actual_shipping_cost") @db.Decimal(15, 2)
  status              SalesOrderStatus    @default(DRAFT)
  productInstances    ProductInstance[]
  details             SalesOrderDetail[]
  agent               Agent               @relation(fields: [agentId], references: [agentId])
  approver            Employee?           @relation("SOApprover", fields: [approverId], references: [employeeId])
  employee            Employee            @relation(fields: [employeeId], references: [employeeId])

  @@map("sales_orders")
}

model SalesOrderDetail {
  salesOrderId       Int                 @map("sales_order_id")
  productId          Int                 @map("product_id")
  quantity           Int
  salePrice          Decimal             @map("sale_price") @db.Decimal(10, 2)
  quantityShipped    Int                 @default(0) @map("quantity_shipped")
  soDetailId         Int                 @id @default(autoincrement()) @map("so_detail_id")
  product            Product             @relation(fields: [productId], references: [productId])
  salesOrder         SalesOrder          @relation(fields: [salesOrderId], references: [salesOrderId], onDelete: Cascade)
  productionRequests ProductionRequest[]

  @@unique([salesOrderId, productId])
  @@map("sales_order_details")
}

model ProductionRequest {
  productionRequestId   Int                     @id @default(autoincrement()) @map("production_request_id")
  requestDate           DateTime                @default(now()) @map("request_date")
  quantity              Int
  employeeId            Int                     @map("employee_id")
  productId             Int                     @map("product_id")
  createdAt             DateTime                @default(now()) @map("created_at")
  updatedAt             DateTime                @updatedAt @map("updated_at")
  priority              Priority                @default(MEDIUM)
  code                  String                  @unique
  soDetailId            Int?                    @map("so_detail_id")
  status                ProductionRequestStatus
  dueDate               DateTime?               @map("due_date")
  note                  String?
  employee              Employee                @relation(fields: [employeeId], references: [employeeId])
  product               Product                 @relation(fields: [productId], references: [productId])
  salesOrderDetail      SalesOrderDetail?       @relation(fields: [soDetailId], references: [soDetailId])
  workOrderFulfillments WorkOrderFulfillment[]
  purchaseOrderDetails  PurchaseOrderDetail[]
  details               ProductionRequestDetail[]

  @@map("production_requests")
}

model ProductionRequestDetail {
  prDetailId          Int               @id @default(autoincrement()) @map("pr_detail_id")
  productionRequestId Int               @map("production_request_id")
  componentId         Int               @map("component_id")
  quantityPerUnit     Int               @map("quantity_per_unit")
  totalRequired       Int               @map("total_required")

  productionRequest   ProductionRequest @relation(fields: [productionRequestId], references: [productionRequestId], onDelete: Cascade)
  component           Component         @relation(fields: [componentId], references: [componentId])

  @@unique([productionRequestId, componentId])
  @@map("production_request_details")
}

model ProductionLine {
  productionLineId  Int               @id @default(autoincrement()) @map("production_line_id")
  lineName          String            @map("line_name")
  location          String?
  createdAt         DateTime          @default(now()) @map("created_at")
  updatedAt         DateTime          @updatedAt @map("updated_at")
  productionBatches ProductionBatch[]
  workOrders        WorkOrder[]

  @@map("production_lines")
}

model WorkOrder {
  workOrderId           Int                     @id @default(autoincrement()) @map("work_order_id")
  createDate            DateTime                @default(now()) @map("create_date")
  employeeId            Int                     @map("employee_id")
  createdAt             DateTime                @default(now()) @map("created_at")
  updatedAt             DateTime                @updatedAt @map("updated_at")
  productId             Int                     @map("product_id")
  code                  String                  @unique
  quantity              Int
  productionLineId      Int?                    @map("production_line_id")
  status                WorkOrderStatus         @default(PLANNED)
  endDate               DateTime?               @map("end_date")
  note                  String?
  startDate             DateTime?               @map("start_date")
  targetDate            DateTime?               @map("target_date")
  materialRequests      MaterialExportRequest[]
  productionBatches     ProductionBatch[]
  productionCosts       ProductionCost[]
  workOrderFulfillments WorkOrderFulfillment[]
  employee              Employee                @relation(fields: [employeeId], references: [employeeId])
  product               Product                 @relation(fields: [productId], references: [productId])
  productionLine        ProductionLine?         @relation(fields: [productionLineId], references: [productionLineId])

  @@map("work_orders")
}

model WorkOrderFulfillment {
  workOrderId         Int               @map("work_order_id")
  productionRequestId Int               @map("production_request_id")
  quantity            Int               @default(0)
  productionRequest   ProductionRequest @relation(fields: [productionRequestId], references: [productionRequestId], onDelete: Cascade)
  workOrder           WorkOrder         @relation(fields: [workOrderId], references: [workOrderId], onDelete: Cascade)

  @@id([workOrderId, productionRequestId])
  @@map("work_order_fulfillments")
}

model ProductionBatch {
  productionBatchId Int               @id @default(autoincrement()) @map("production_batch_id")
  batchCode         String            @unique @map("batch_code")
  productionDate    DateTime          @map("production_date")
  expiryDate        DateTime?         @map("expiry_date")
  workOrderId       Int               @map("work_order_id")
  productionLineId  Int?              @map("production_line_id")
  createdAt         DateTime          @default(now()) @map("created_at")
  updatedAt         DateTime          @updatedAt @map("updated_at")
  productInstances  ProductInstance[]
  productionLine    ProductionLine?   @relation(fields: [productionLineId], references: [productionLineId])
  workOrder         WorkOrder         @relation(fields: [workOrderId], references: [workOrderId])

  @@map("production_batches")
}

model ProductInstance {
  productInstanceId     Int                    @id @default(autoincrement()) @map("product_instance_id")
  serialNumber          String                 @unique @map("serial_number")
  unitProductionCost    Decimal?               @map("unit_production_cost") @db.Decimal(10, 2)
  productId             Int                    @map("product_id")
  productionBatchId     Int                    @map("production_batch_id")
  createdAt             DateTime               @default(now()) @map("created_at")
  updatedAt             DateTime               @updatedAt @map("updated_at")
  status                ProductInstanceStatus  @default(WIP)
  salesOrderId          Int?                   @map("sales_order_id")
  warehouseId           Int?                   @map("warehouse_id")
  inventoryTransactions InventoryTransaction[]
  product               Product                @relation(fields: [productId], references: [productId])
  productionBatch       ProductionBatch        @relation(fields: [productionBatchId], references: [productionBatchId])
  salesOrder            SalesOrder?            @relation(fields: [salesOrderId], references: [salesOrderId])
  warehouse             Warehouse?             @relation(fields: [warehouseId], references: [warehouseId])
  qualityChecks         QualityCheck[]
  warranty              Warranty?

  @@map("product_instances")
}

model Warranty {
  warrantyId        Int             @id @default(autoincrement()) @map("warranty_id")
  activationDate    DateTime        @map("activation_date")
  expiryDate        DateTime        @map("expiry_date")
  productInstanceId Int             @unique @map("product_instance_id")
  customerId        Int             @map("customer_id")
  createdAt         DateTime        @default(now()) @map("created_at")
  updatedAt         DateTime        @updatedAt @map("updated_at")
  customer          Customer        @relation(fields: [customerId], references: [customerId])
  productInstance   ProductInstance @relation(fields: [productInstanceId], references: [productInstanceId])

  @@map("warranties")
}

model QualityChecklist {
  checklistId   Int            @id @default(autoincrement()) @map("checklist_id")
  checklistName String         @map("checklist_name")
  description   String?
  createdAt     DateTime       @default(now()) @map("created_at")
  updatedAt     DateTime       @updatedAt @map("updated_at")
  qualityChecks QualityCheck[]

  @@map("quality_checklists")
}

model QualityCheck {
  qualityCheckId    Int                @id @default(autoincrement()) @map("quality_check_id")
  checkDate         DateTime           @default(now()) @map("check_date")
  notes             String?
  employeeId        Int                @map("employee_id")
  productInstanceId Int                @map("product_instance_id")
  checklistId       Int                @map("checklist_id")
  createdAt         DateTime           @default(now()) @map("created_at")
  updatedAt         DateTime           @updatedAt @map("updated_at")
  result            QualityCheckResult
  checklist         QualityChecklist   @relation(fields: [checklistId], references: [checklistId])
  employee          Employee           @relation(fields: [employeeId], references: [employeeId])
  productInstance   ProductInstance    @relation(fields: [productInstanceId], references: [productInstanceId])

  @@map("quality_checks")
}

model Warehouse {
  warehouseId           Int                    @id @default(autoincrement()) @map("warehouse_id")
  warehouseName         String                 @map("warehouse_name")
  location              String?
  warehouseType         WarehouseType          @map("warehouse_type")
  createdAt             DateTime               @default(now()) @map("created_at")
  updatedAt             DateTime               @updatedAt @map("updated_at")
  code                  String                 @unique
  componentStocks       ComponentStock[]
  inventoryTransactions InventoryTransaction[]
  stocktakes            Stocktake[]
  productInstances      ProductInstance[]
  componentLots         ComponentLot[]
  purchaseOrders        PurchaseOrder[]

  @@map("warehouses")
}

model ComponentStock {
  warehouseId       Int       @map("warehouse_id")
  componentId       Int       @map("component_id")
  quantity          Int
  updatedAt         DateTime  @updatedAt @map("updated_at")
  component         Component @relation(fields: [componentId], references: [componentId])
  warehouse         Warehouse @relation(fields: [warehouseId], references: [warehouseId])

  @@id([warehouseId, componentId])
  @@map("component_stocks")
}

model InventoryTransaction {
  transactionId     Int                      @id @default(autoincrement()) @map("transaction_id")
  transactionDate   DateTime                 @default(now()) @map("transaction_date")
  quantity          Int
  note              String?
  employeeId        Int                      @map("employee_id")
  warehouseId       Int                      @map("warehouse_id")
  componentId       Int?                     @map("component_id")
  productInstanceId Int?                     @map("product_instance_id")
  transactionType   InventoryTransactionType @map("transaction_type")
  materialReqId     Int?                     @map("material_req_id")
  purchaseOrderId   Int?                     @map("purchase_order_id")
  component         Component?               @relation(fields: [componentId], references: [componentId])
  employee          Employee                 @relation(fields: [employeeId], references: [employeeId])
  materialReq       MaterialExportRequest?   @relation(fields: [materialReqId], references: [requestId])
  productInstance   ProductInstance?         @relation(fields: [productInstanceId], references: [productInstanceId])
  purchaseOrder     PurchaseOrder?           @relation(fields: [purchaseOrderId], references: [purchaseOrderId])
  warehouse         Warehouse                @relation(fields: [warehouseId], references: [warehouseId])

  @@index([transactionType])
  @@index([transactionDate])
  @@map("inventory_transactions")
}

model CostCategory {
  costCategoryId  Int              @id @default(autoincrement()) @map("cost_category_id")
  categoryName    String           @map("category_name")
  description     String?
  createdAt       DateTime         @default(now()) @map("created_at")
  updatedAt       DateTime         @updatedAt @map("updated_at")
  productionCosts ProductionCost[]

  @@map("cost_categories")
}

model ProductionCost {
  productionCostId Int          @id @default(autoincrement()) @map("production_cost_id")
  amount           Decimal      @db.Decimal(12, 2)
  description      String?
  workOrderId      Int          @map("work_order_id")
  costCategoryId   Int          @map("cost_category_id")
  createdAt        DateTime     @default(now()) @map("created_at")
  costCategory     CostCategory @relation(fields: [costCategoryId], references: [costCategoryId])
  workOrder        WorkOrder    @relation(fields: [workOrderId], references: [workOrderId], onDelete: Cascade)

  @@map("production_costs")
}

model SupplierComponent {
  supplierId  Int       @map("supplier_id")
  componentId Int       @map("component_id")
  createdAt   DateTime  @default(now()) @map("created_at")
  component   Component @relation(fields: [componentId], references: [componentId], onDelete: Cascade)
  supplier    Supplier  @relation(fields: [supplierId], references: [supplierId], onDelete: Cascade)

  @@id([supplierId, componentId])
  @@map("supplier_components")
}

model MaterialExportRequest {
  requestId    Int                     @id @default(autoincrement()) @map("material_req_id")
  code         String                  @unique
  requestDate  DateTime                @default(now()) @map("request_date")
  status       RequestStatus           @default(PENDING)
  note         String?
  workOrderId  Int                     @map("work_order_id")
  requesterId  Int                     @map("requester_id")
  approverId   Int?                    @map("approver_id")
  transactions InventoryTransaction[]
  approver     Employee?               @relation("Approver", fields: [approverId], references: [employeeId])
  requester    Employee                @relation("Requester", fields: [requesterId], references: [employeeId])
  workOrder    WorkOrder               @relation(fields: [workOrderId], references: [workOrderId])
  details      MaterialRequestDetail[]

  @@map("material_export_requests")
}

model MaterialRequestDetail {
  detailId    Int                   @id @default(autoincrement())
  requestId   Int                   @map("material_req_id")
  componentId Int                   @map("component_id")
  quantity    Int
  component   Component             @relation(fields: [componentId], references: [componentId])
  request     MaterialExportRequest @relation(fields: [requestId], references: [requestId], onDelete: Cascade)

  @@map("material_request_details")
}

model Notification {
  notificationId    Int              @id @default(autoincrement()) @map("notification_id")
  type              NotificationType
  title             String
  message           String
  isRead            Boolean          @default(false) @map("is_read")
  employeeId        Int              @map("employee_id")
  relatedEntityType String?          @map("related_entity_type")
  relatedEntityId   Int?             @map("related_entity_id")
  createdAt         DateTime         @default(now()) @map("created_at")
  employee          Employee         @relation(fields: [employeeId], references: [employeeId], onDelete: Cascade)

  @@index([employeeId, isRead])
  @@index([createdAt])
  @@map("notifications")
}

model Stocktake {
  stocktakeId Int             @id @default(autoincrement()) @map("stocktake_id")
  code        String          @unique
  status      String          @default("DRAFT")
  warehouseId Int             @map("warehouse_id")
  description String?
  startedAt   DateTime?       @map("started_at")
  completedAt DateTime?       @map("completed_at")
  createdBy   Int             @map("created_by")
  createdAt   DateTime        @default(now()) @map("created_at")
  updatedAt   DateTime        @updatedAt @map("updated_at")
  items       StocktakeItem[]
  creator     Employee        @relation("StocktakeCreator", fields: [createdBy], references: [employeeId])
  warehouse   Warehouse       @relation(fields: [warehouseId], references: [warehouseId])

  @@map("stocktaking")
}

model StocktakeItem {
  stocktakeItemId Int       @id @default(autoincrement()) @map("stocktake_item_id")
  stocktakeId     Int       @map("stocktake_id")
  componentId     Int       @map("component_id")
  systemQuantity  Int       @map("system_quantity")
  actualQuantity  Int?      @map("actual_quantity")
  notes           String?
  component       Component @relation(fields: [componentId], references: [componentId])
  stocktake       Stocktake @relation(fields: [stocktakeId], references: [stocktakeId], onDelete: Cascade)

  @@unique([stocktakeId, componentId])
  @@map("stocktake_items")
}

model CodeSequence {
  scope        String   @id
  currentValue Int      @default(0) @map("current_value")

  @@map("code_sequences")
}

enum Priority {
  HIGH
  MEDIUM
  LOW
}

enum AttachmentCategory {
  CONTRACT
  INVOICE
  PACKING_SLIP
  INSPECTION
  OTHER
}

model Attachment {
  attachmentId Int                @id @default(autoincrement()) @map("attachment_id")
  entityType   String             @map("entity_type")  // "PURCHASE_ORDER", "WORK_ORDER", "QUALITY_CHECK"
  entityId     Int                @map("entity_id")    // FK-like reference to the parent entity PK
  fileKey      String             @map("file_key")     // R2 object key, e.g. "purchase-orders/47/invoice.pdf"
  fileName     String             @map("file_name")    // Original filename for display
  fileSize     Int                @map("file_size")    // Bytes
  mimeType     String             @map("mime_type")    // "application/pdf", "video/mp4", etc.
  category     AttachmentCategory @default(OTHER)
  uploadedBy   Int                @map("uploaded_by")
  uploadedAt   DateTime           @default(now()) @map("uploaded_at")

  employee     Employee           @relation(fields: [uploadedBy], references: [employeeId])

  @@index([entityType, entityId])
  @@map("attachments")
}

enum SalesOrderStatus {
  DRAFT
  PENDING_APPROVAL
  APPROVED
  IN_PROGRESS
  COMPLETED
  CANCELLED
  RETURNED
}

enum PurchaseOrderStatus {
  DRAFT
  PENDING
  APPROVED
  ORDERED
  RECEIVING
  COMPLETED
  CANCELLED
}

enum ProductionRequestStatus {
  APPROVED
  WAITING_MATERIAL
  PARTIALLY_FULFILLED
  FULFILLED
  CANCELLED
}

enum WorkOrderStatus {
  PLANNED
  RELEASED
  IN_PROGRESS
  ON_HOLD
  COMPLETED
  CLOSED
  CANCELLED
}

enum QualityCheckResult {
  PASSED
  FAILED
  NEEDS_REWORK
}

enum ProductInstanceStatus {
  WIP
  IN_STOCK
  SHIPPED
  RETURNED
  DEFECTIVE
  SCRAPPED
}

enum EmployeeStatus {
  ACTIVE
  INACTIVE
}

enum InventoryTransactionType {
  IMPORT_PO
  EXPORT_PRODUCTION
  IMPORT_PRODUCTION
  EXPORT_SALES
  TRANSFER
  ADJUSTMENT
  RETURN_IN
  RETURN_OUT
  SCRAP
}

enum RequestStatus {
  PENDING
  APPROVED
  REJECTED
  COMPLETED
}

enum WarehouseType {
  COMPONENT
  SALES
  ERROR
}

enum NotificationType {
  PO_APPROVED
  PO_RECEIVED
  SO_APPROVED
  SO_SHIPPED
  PR_APPROVED
  WO_COMPLETED
  LOW_STOCK
  MATERIAL_ISSUED
  GENERAL
}
